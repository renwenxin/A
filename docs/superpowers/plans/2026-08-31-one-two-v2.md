# 1进2 精选优化（今日1进2 · 视频方法论版）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按逻辑哥视频方法论实现独立的"今日1进2"选股系统（盘后 8 维打分精选 + 次日竞价确认 + 结果台账与维度命中率 + 权重可配），成品放交易流程新页面，零侵入现有代码（复盘/OneTwoScreener 不动）。

**Architecture:** 新包 `ashare_review/one_two_v2/`（weights=可配权重 / picks=8 维打分+战法分类 / auction=竞价确认 / ledger=SQLite 台账+维度统计 / service=编排+后台任务），复用项目现有数据源（涨停池/竞价/TDX/concept_map/market_state），不改任何现有模块。

**Tech Stack:** Python 3 + sqlite3 + Flask + pytest + pandas（测试 FakeTdx 用）。

**设计依据:** `docs/superpowers/specs/2026-08-31-one-two-v2-design.md`（commit d9e91db）

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `ashare_review/one_two_v2/__init__.py` | 包标记 |
| `ashare_review/one_two_v2/weights.py` | DEFAULT_WEIGHTS + validate + WeightStore（原子写/损坏回退） |
| `ashare_review/one_two_v2/picks.py` | 8 维打分纯函数 + 战法分类 + run_picks（数据组装） |
| `ashare_review/one_two_v2/auction.py` | 竞价确认：爆量比三档 + 战法联动 |
| `ashare_review/one_two_v2/ledger.py` | SQLite 台账 + 记录/验证/维度命中率统计 |
| `ashare_review/one_two_v2/service.py` | run_picks/refresh_auction/verify_pending + 后台任务 JOBS |
| `ashare_review/web/app.py` | 修改：6 个 API + 页面路由 |
| `ashare_review/web/templates/base.html` | 修改：交易流程导航加「🧩 今日1进2」 |
| `ashare_review/web/templates/one_two_picks.html` | 新建：页面 |
| `ashare_review/tests/test_one_two_v2.py` | 全部新测试 |

**关键约定：**
- 候选池：今日涨停池中首板（consecutive==1）、非一字板（_is_yizi_board 逻辑）、非 ST、非北交所（8/4/92 前缀排除）
- 8 维默认权重：quality 30 / theme_stage 15 / emotion 10 / energy_ladder 10 / volume_health 15 / theme_overlay 8 / cap_price 7 / status 5
- 阈值默认：auction_ratio_high 10 / mid 5 / low 3 / volume_health_pct 80 / cap_max 100 / price_max 15
- 台账命中口径（与预测台账一致）：次日涨停(zt) 或涨幅≥3%(up3) 记命中
- 战法：'auction'（默认竞价确认）/ 'graph'（跳空过前高形态）/ 'weak_strong'（烂板/尾盘封板）

---

### Task 1: weights.py 默认权重 + 校验 + 存储

**Files:**
- Create: `ashare_review/one_two_v2/__init__.py`（空）
- Create: `ashare_review/one_two_v2/weights.py`
- Create: `ashare_review/tests/test_one_two_v2.py`

- [ ] **Step 1: 写失败测试**

```python
# ashare_review/tests/test_one_two_v2.py
"""今日1进2（视频方法论版）单元测试"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))


# ---------- Task 1: 权重 ----------

def test_default_weights_complete():
    from ashare_review.one_two_v2.weights import DEFAULT_WEIGHTS, DIMENSIONS
    assert set(DEFAULT_WEIGHTS['dimensions'].keys()) == set(DIMENSIONS)
    assert len(DIMENSIONS) == 8
    for k in ('auction_ratio_high', 'auction_ratio_mid', 'auction_ratio_low',
              'volume_health_pct', 'cap_max', 'price_max'):
        assert k in DEFAULT_WEIGHTS['thresholds']
    assert DEFAULT_WEIGHTS['thresholds']['auction_ratio_high'] == 10.0
    assert DEFAULT_WEIGHTS['thresholds']['volume_health_pct'] == 80.0


def test_validate_weights():
    from ashare_review.one_two_v2.weights import validate_weights, DEFAULT_WEIGHTS
    assert validate_weights(DEFAULT_WEIGHTS) == []
    # 维度越界
    bad = dict(DEFAULT_WEIGHTS)
    bad['dimensions'] = dict(bad['dimensions'], quality=60)
    assert any('quality' in e for e in validate_weights(bad))
    # 阈值非法
    bad2 = dict(DEFAULT_WEIGHTS)
    bad2['thresholds'] = dict(bad2['thresholds'], auction_ratio_high=-1)
    assert any('auction_ratio_high' in e for e in validate_weights(bad2))
    # 缺字段
    bad3 = {'dimensions': {}, 'thresholds': {}}
    assert validate_weights(bad3) != []


def test_weight_store(tmp_path):
    from ashare_review.one_two_v2.weights import WeightStore
    path = str(tmp_path / 'w.json')
    s = WeightStore(path)
    assert s.get()['dimensions']['quality'] == 30      # 缺文件回退默认
    s.set({'dimensions': {'quality': 40}, 'thresholds': {'auction_ratio_low': 2.0}})
    s2 = WeightStore(path)
    w = s2.get()
    assert w['dimensions']['quality'] == 40            # 缺字段合并默认
    assert w['thresholds']['auction_ratio_low'] == 2.0
    # 损坏回退
    bad = tmp_path / 'bad.json'
    bad.write_text('{broken', encoding='utf-8')
    assert WeightStore(str(bad)).get()['dimensions']['quality'] == 30
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: FAIL — ModuleNotFoundError

- [ ] **Step 3: 实现 weights.py**

```python
# ashare_review/one_two_v2/weights.py
"""今日1进2 — 可配权重与阈值（视频方法论默认值，页面可调）"""
import json
import logging
import os
import tempfile
import threading
from typing import Dict, List, Optional

logger = logging.getLogger(__name__)

DIMENSIONS = ['quality', 'theme_stage', 'emotion', 'energy_ladder',
              'volume_health', 'theme_overlay', 'cap_price', 'status']

DEFAULT_WEIGHTS: Dict[str, Dict] = {
    'dimensions': {
        'quality': 30, 'theme_stage': 15, 'emotion': 10, 'energy_ladder': 10,
        'volume_health': 15, 'theme_overlay': 8, 'cap_price': 7, 'status': 5,
    },
    'thresholds': {
        'auction_ratio_high': 10.0, 'auction_ratio_mid': 5.0, 'auction_ratio_low': 3.0,
        'volume_health_pct': 80.0, 'cap_max': 100.0, 'price_max': 15.0,
    },
}

_WEIGHTS_PATH = os.environ.get(
    'ONE_TWO_WEIGHTS',
    os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
                 'data', 'one_two_weights.json'))
_LOCK = threading.Lock()


def _merge_default(saved: Optional[dict]) -> dict:
    base = {'dimensions': dict(DEFAULT_WEIGHTS['dimensions']),
            'thresholds': dict(DEFAULT_WEIGHTS['thresholds'])}
    if not isinstance(saved, dict):
        return base
    if isinstance(saved.get('dimensions'), dict):
        for k, v in saved['dimensions'].items():
            if k in base['dimensions'] and isinstance(v, (int, float)) and not isinstance(v, bool):
                base['dimensions'][k] = v
    if isinstance(saved.get('thresholds'), dict):
        for k, v in saved['thresholds'].items():
            if k in base['thresholds'] and isinstance(v, (int, float)) and not isinstance(v, bool):
                base['thresholds'][k] = v
    return base


def validate_weights(weights: dict) -> List[str]:
    errors = []
    dims = weights.get('dimensions')
    if not isinstance(dims, dict):
        return ['dimensions 必须为对象']
    for k in DIMENSIONS:
        v = dims.get(k)
        if not isinstance(v, (int, float)) or isinstance(v, bool) or not (0 <= v <= 50):
            errors.append(f'dimensions.{k} 需在 0~50 之间（当前 {v}）')
    th = weights.get('thresholds')
    if not isinstance(th, dict):
        errors.append('thresholds 必须为对象')
    else:
        for k in ('auction_ratio_high', 'auction_ratio_mid', 'auction_ratio_low',
                  'volume_health_pct', 'cap_max', 'price_max'):
            v = th.get(k)
            if not isinstance(v, (int, float)) or isinstance(v, bool) or v <= 0:
                errors.append(f'thresholds.{k} 必须 >0（当前 {v}）')
        if all(isinstance(th.get(k), (int, float)) for k in ('auction_ratio_high', 'auction_ratio_mid', 'auction_ratio_low')):
            if not (th['auction_ratio_low'] <= th['auction_ratio_mid'] <= th['auction_ratio_high']):
                errors.append('thresholds 需 low ≤ mid ≤ high')
    return errors


class WeightStore:
    def __init__(self, path: Optional[str] = None):
        self.path = path or _WEIGHTS_PATH

    def _load_raw(self) -> dict:
        try:
            if os.path.exists(self.path):
                with open(self.path, 'r', encoding='utf-8') as f:
                    data = json.load(f)
                if isinstance(data, dict):
                    return data
        except Exception as e:
            logger.warning('1进2 权重读取失败 %s（%s），回退默认', self.path, e)
        return {}

    def get(self) -> dict:
        return _merge_default(self._load_raw().get('weights'))

    def set(self, weights: dict) -> None:
        full = _merge_default(weights)
        errors = validate_weights(full)
        if errors:
            raise ValueError('; '.join(errors))
        os.makedirs(os.path.dirname(self.path) or '.', exist_ok=True)
        with _LOCK:
            fd, tmp = tempfile.mkstemp(dir=os.path.dirname(self.path) or '.',
                                       suffix='.tmp', prefix='one_two_weights_')
            try:
                with os.fdopen(fd, 'w', encoding='utf-8') as f:
                    json.dump({'weights': full}, f, ensure_ascii=False, indent=2)
                os.replace(tmp, self.path)
            except Exception:
                if os.path.exists(tmp):
                    os.remove(tmp)
                raise
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: 3 passed

- [ ] **Step 5: 提交**

```bash
git add ashare_review/one_two_v2 ashare_review/tests/test_one_two_v2.py
git commit -m "feat(1in2): 可配权重与阈值（默认=视频方法论）"
```

---

### Task 2: picks.py 8 维打分 + 战法分类

**Files:**
- Create: `ashare_review/one_two_v2/picks.py`
- Test: `ashare_review/tests/test_one_two_v2.py`（追加）

- [ ] **Step 1: 写失败测试（打分纯函数 + 候选过滤 + 战法分类）**

```python
# 追加到 test_one_two_v2.py

# ---------- Task 2: 8 维打分 ----------

def _lu(code='600001', name='测试', t='10:00', cons=1, seal=True, broken=False,
        seal_amt=5000.0, turnover=8000.0, cap=40.0, price=8.0, board='换手板'):
    from ashare_review.data.models import LimitUpInfo
    return LimitUpInfo(code=code, name=name, limit_up_time=t, seal_amount=seal_amt,
                       turnover=turnover, float_market_cap=cap, consecutive=cons,
                       is_first=cons == 1, is_seal=seal, is_broken=broken,
                       board_type=board, close_price=price)


def _weights():
    from ashare_review.one_two_v2.weights import DEFAULT_WEIGHTS
    return DEFAULT_WEIGHTS


def test_candidate_filter():
    from ashare_review.one_two_v2.picks import filter_candidates
    pool = [
        _lu(code='600001', t='09:35', seal_amt=5000, turnover=8000),      # 合格首板
        _lu(code='600002', t='09:25', seal_amt=5000, turnover=8000),      # 一字板排除
        _lu(code='600003', cons=3),                                        # 3板排除
        _lu(code='000001', t='10:00'),                                     # 深主板合格
        _lu(code='300001', t='10:00'),                                     # 创业板排除（非主板）
        _lu(code='830001', t='10:00'),                                     # 北交所排除
    ]
    cands = filter_candidates(pool)
    codes = sorted(c['code'] for c in cands)
    assert codes == ['000001', '600001']


def test_score_quality():
    from ashare_review.one_two_v2.picks import score_dimension
    w = _weights()
    # 早盘秒板 + 封成比>0.5 + 封单强度>0.015 + 封死 → 高分
    lu = _lu(t='09:35', seal_amt=6000, turnover=8000, cap=40.0)   # 封成比0.75, 强度 6000/(40e4)=0.015
    r = score_dimension('quality', lu, {}, w)
    assert r['score'] > 20
    # 尾盘封板 → 低分
    lu2 = _lu(t='14:30', seal_amt=2000, turnover=8000)
    r2 = score_dimension('quality', lu2, {}, w)
    assert r2['score'] < r['score']


def test_score_theme_stage():
    from ashare_review.one_two_v2.picks import score_dimension
    w = _weights()
    ctx = {'sector': {'zt_count': 6, 'max_consecutive': 2, 'is_new_theme': True}}
    r = score_dimension('theme_stage', _lu(), ctx, w)
    assert r['score'] > 0 and '试水' in r['reason']
    ctx2 = {'sector': {'zt_count': 9, 'max_consecutive': 5}}
    r2 = score_dimension('theme_stage', _lu(), ctx2, w)
    assert r2['score'] < 0 and '兑现' in r2['reason']


def test_score_volume_health():
    from ashare_review.one_two_v2.picks import score_dimension
    w = _weights()
    # 今日量/前高量 = 0.8 → 最佳区间
    r = score_dimension('volume_health', _lu(), {'today_vol': 800, 'prev_high_vol': 1000}, w)
    assert r['score'] > 0
    # 极致缩量 0.2 → 负分
    r2 = score_dimension('volume_health', _lu(), {'today_vol': 200, 'prev_high_vol': 1000}, w)
    assert r2['score'] < 0
    # 爆量 2.0 → 负分
    r3 = score_dimension('volume_health', _lu(), {'today_vol': 2000, 'prev_high_vol': 1000}, w)
    assert r3['score'] < 0


def test_score_emotion_energy_status():
    from ashare_review.one_two_v2.picks import score_dimension
    w = _weights()
    # 二连冰 → +分
    r = score_dimension('emotion', _lu(), {'zt_trend': 'double_ice'}, w)
    assert r['score'] > 0
    # 高潮次日 → -分
    r2 = score_dimension('emotion', _lu(), {'zt_trend': 'double_climax'}, w)
    assert r2['score'] < 0
    # 高能量梯队在2板 → +分
    r3 = score_dimension('energy_ladder', _lu(), {'ladder_at_2': True}, w)
    assert r3['score'] > 0
    # 资金攻3板 → -分
    r4 = score_dimension('energy_ladder', _lu(), {'ladder_at_2': False, 'ladder_at_3': True}, w)
    assert r4['score'] < 0
    # 上方无同题材高位 → +分；有 → -分
    r5 = score_dimension('status', _lu(), {'upper_same_theme': False}, w)
    assert r5['score'] > 0
    r6 = score_dimension('status', _lu(), {'upper_same_theme': True}, w)
    assert r6['score'] < 0


def test_tactic_classify():
    from ashare_review.one_two_v2.picks import classify_tactic
    # 尾盘封板 → weak_strong
    assert classify_tactic(_lu(t='14:30')) == 'weak_strong'
    # 炸板回封 → weak_strong
    assert classify_tactic(_lu(t='10:00', seal=True, broken=True)) == 'weak_strong'
    # 早盘健康封板 → graph 或 auction
    t = classify_tactic(_lu(t='09:40'))
    assert t in ('graph', 'auction')
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: FAIL — ModuleNotFoundError/AttributeError

- [ ] **Step 3: 实现 picks.py**

```python
# ashare_review/one_two_v2/picks.py
"""今日1进2 — 盘后 8 维打分 + 战法分类（视频方法论）"""
from typing import Dict, List, Optional, Tuple


def filter_candidates(pool: List) -> List[Dict]:
    """候选池：首板、非一字板、非ST、非北交所。返回 [{lu, code, name}]"""
    cands = []
    for lu in pool:
        code = str(lu.code)
        if lu.consecutive != 1:
            continue
        if code.startswith(('8', '4', '92', '30', '68')):
            continue   # 主板优先（排除北交/创业板/科创板）
        try:
            if str(lu.name).startswith(('ST', '*ST', 'SST', 'S*ST')):
                continue
        except Exception:
            pass
        try:
            t = str(lu.limit_up_time).replace(':', '')[:4]
            if t == '0925':   # 一字板
                continue
        except Exception:
            pass
        cands.append({'lu': lu, 'code': code, 'name': lu.name})
    return cands


def _parse_time(lu) -> int:
    try:
        return int(str(lu.limit_up_time).replace(':', '')[:4])
    except (ValueError, TypeError):
        return 1400


def score_dimension(dim: str, lu, ctx: dict, weights: dict) -> Dict:
    """单维打分，返回 {'score': int, 'reason': str}。ctx 为上下文 dict。"""
    w = weights['dimensions'].get(dim, 0)
    if dim == 'quality':
        t = _parse_time(lu)
        s = 0
        if t <= 1030:
            s += 10
        elif t <= 1130:
            s += 6
        elif t <= 1400:
            s += 2
        else:
            s -= 4
        if lu.turnover and lu.seal_amount:
            sr = lu.seal_amount / lu.turnover
            if sr > 0.5:
                s += 10
            elif sr > 0.3:
                s += 4
        if lu.float_market_cap and lu.seal_amount:
            ss = lu.seal_amount / (lu.float_market_cap * 10000)
            if ss > 0.015:
                s += 6
        if lu.is_seal and not lu.is_broken:
            s += 4
        return {'score': round(s * w / 30, 1) if w else 0, 'reason': f'质量{s}'}
    if dim == 'theme_stage':
        sector = ctx.get('sector') or {}
        zt = sector.get('zt_count', 0)
        cons = sector.get('max_consecutive', 1)
        if zt >= 8 and cons >= 4:
            return {'score': -w, 'reason': '兑现期·追高危险'}
        if zt >= 5 or (zt >= 3 and cons >= 2):
            return {'score': w, 'reason': '试水期·爆发力最强'}
        if zt <= 2:
            return {'score': -w * 0.5, 'reason': '朦胧期·一日游风险'}
        return {'score': 0, 'reason': '题材中性'}
    if dim == 'emotion':
        trend = ctx.get('zt_trend', '')
        if trend == 'double_ice':
            return {'score': w, 'reason': '二连冰·情绪转暖前夜'}
        if trend == 'double_climax':
            return {'score': -w, 'reason': '连高两日·高潮次日回避'}
        return {'score': 0, 'reason': '情绪中性'}
    if dim == 'energy_ladder':
        if ctx.get('ladder_at_2'):
            return {'score': w, 'reason': '高能量梯队在2板'}
        if ctx.get('ladder_at_3'):
            return {'score': -w * 0.5, 'reason': '资金主攻3板·1进2弱化'}
        return {'score': 0, 'reason': '梯队中性'}
    if dim == 'volume_health':
        tv = ctx.get('today_vol')
        pv = ctx.get('prev_high_vol')
        pct = weights['thresholds'].get('volume_health_pct', 80.0)
        if tv and pv and pv > 0:
            ratio = tv / pv
            if 0.7 <= ratio <= 1.0:
                return {'score': w, 'reason': f'量能健康({ratio:.0%}前高量)'}
            if ratio < 0.4 or ratio > 1.5:
                return {'score': -w * 0.5, 'reason': f'量能极端({ratio:.0%})'}
            return {'score': w * 0.5, 'reason': f'量能尚可({ratio:.0%})'}
        return {'score': 0, 'reason': '量能数据缺失'}
    if dim == 'theme_overlay':
        n = ctx.get('concept_count', 0)
        if n >= 3:
            return {'score': w, 'reason': f'题材叠加({n}概念)·万金油'}
        if n >= 1:
            return {'score': w * 0.5, 'reason': f'{n}概念'}
        return {'score': -w * 0.25, 'reason': '题材孤立'}
    if dim == 'cap_price':
        cap = lu.float_market_cap or 0
        price = lu.close_price or 0
        cap_max = weights['thresholds'].get('cap_max', 100.0)
        price_max = weights['thresholds'].get('price_max', 15.0)
        if cap <= 50 and price <= price_max:
            return {'score': w, 'reason': f'市值{cap:.0f}亿·价{price:.0f}·轿子轻'}
        if cap <= cap_max:
            return {'score': w * 0.5, 'reason': '市值适中'}
        return {'score': -w * 0.5, 'reason': f'市值{cap:.0f}亿偏大'}
    if dim == 'status':
        if ctx.get('upper_same_theme'):
            return {'score': -w, 'reason': '上方有同题材高位·后排套利'}
        return {'score': w, 'reason': '无同题材高位·补涨龙机会'}
    return {'score': 0, 'reason': ''}


def classify_tactic(lu) -> str:
    """战法分类：weak_strong(烂板/尾盘) / graph(图形突破候选) / auction(竞价爆量)"""
    if _parse_time(lu) >= 1400 or (lu.is_broken and lu.is_seal):
        return 'weak_strong'
    return 'auction'


def compute_score(lu, ctx: dict, weights: dict) -> Dict:
    """8 维加权总分。返回 {score, dimensions: {dim: {score, reason}}, tactic}"""
    dims = {}
    total = 0.0
    for dim in ('quality', 'theme_stage', 'emotion', 'energy_ladder',
                'volume_health', 'theme_overlay', 'cap_price', 'status'):
        r = score_dimension(dim, lu, ctx, weights)
        dims[dim] = r
        total += r['score']
    return {'score': round(total, 1), 'dimensions': dims,
            'tactic': classify_tactic(lu)}
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: 8 passed（3 + 5）

- [ ] **Step 5: 提交**

```bash
git add ashare_review/one_two_v2/picks.py ashare_review/tests/test_one_two_v2.py
git commit -m "feat(1in2): 8 维打分引擎（视频方法论）与战法分类"
```

---

### Task 3: auction.py 竞价确认

**Files:**
- Create: `ashare_review/one_two_v2/auction.py`
- Test: `ashare_review/tests/test_one_two_v2.py`（追加）

- [ ] **Step 1: 写失败测试**

```python
# 追加到 test_one_two_v2.py

# ---------- Task 3: 竞价确认 ----------

def test_auction_ratio_tiers():
    from ashare_review.one_two_v2.auction import grade_auction_ratio
    from ashare_review.one_two_v2.weights import DEFAULT_WEIGHTS
    w = DEFAULT_WEIGHTS['thresholds']
    # 竞价额(万) / 市值(亿×1e4 万)：40亿市值 → 4亿竞价=10%、2亿=5%、1.2亿=3%
    assert grade_auction_ratio(40000.0, 40.0, w)['level'] == 'extreme'
    assert grade_auction_ratio(20000.0, 40.0, w)['level'] == 'high'
    assert grade_auction_ratio(12000.0, 40.0, w)['level'] == 'mid'
    assert grade_auction_ratio(1000.0, 40.0, w)['level'] == 'low'


def test_auction_tactic_trigger():
    from ashare_review.one_two_v2.auction import check_trigger
    # weak_strong：高开≥3% 且竞价量>昨日最高量50% → 触发
    r = check_trigger('weak_strong', open_change_pct=4.0,
                      auction_volume=600, preclose_volume=1000, prev_high=800.0, gap_price=None)
    assert r['triggered'] is True and '弱转强' in r['note']
    # weak_strong 高开不足 → 不触发
    r2 = check_trigger('weak_strong', open_change_pct=1.0,
                       auction_volume=600, preclose_volume=1000, prev_high=800.0, gap_price=None)
    assert r2['triggered'] is False
    # graph：竞价价 > 前高 → 跳空过压力位触发
    r3 = check_trigger('graph', open_change_pct=2.0, auction_volume=0,
                       preclose_volume=0, prev_high=9.5, gap_price=10.2)
    assert r3['triggered'] is True and '图形' in r3['note']
    # graph 未过前高 → 不触发
    r4 = check_trigger('graph', open_change_pct=2.0, auction_volume=0,
                       preclose_volume=0, prev_high=10.5, gap_price=10.2)
    assert r4['triggered'] is False
    # auction：按爆量比等级触发（≥mid 触发）
    r5 = check_trigger('auction', open_change_pct=2.0, auction_volume=0,
                       preclose_volume=0, prev_high=0.0, gap_price=None,
                       ratio=6.0, thresholds={'auction_ratio_low': 3.0})
    assert r5['triggered'] is True
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: FAIL — ModuleNotFoundError

- [ ] **Step 3: 实现 auction.py**

```python
# ashare_review/one_two_v2/auction.py
"""今日1进2 — 次日竞价确认（视频竞价爆量公式 + 战法联动）"""
from typing import Dict, Optional

LEVEL_LABEL = {'extreme': '🔴 爆量极强', 'high': '🟠 爆量', 'mid': '🟡 放量', 'low': '⚪ 平淡'}


def grade_auction_ratio(auction_amount: float, free_mcap_yi: float,
                        thresholds: dict) -> Dict:
    """竞价爆量比 = 竞价成交额 / 自由流通市值 ×100。返回 {level, ratio, label}

    注：auction_amount 单位需与 mcap 一致换算——按万/亿：mcap_yi 亿 = mcap_yi*1e4 万，
    ratio = auction_amount(万) / (mcap_yi * 1e4) * 100。
    """
    if free_mcap_yi <= 0:
        return {'level': 'low', 'ratio': 0.0, 'label': LEVEL_LABEL['low']}
    ratio = auction_amount / (free_mcap_yi * 1e4) * 100
    if ratio >= thresholds.get('auction_ratio_high', 10.0):
        level = 'extreme'
    elif ratio >= thresholds.get('auction_ratio_mid', 5.0):
        level = 'high'
    elif ratio >= thresholds.get('auction_ratio_low', 3.0):
        level = 'mid'
    else:
        level = 'low'
    return {'level': level, 'ratio': round(ratio, 2), 'label': LEVEL_LABEL[level]}


def check_trigger(tactic: str, open_change_pct: float = 0.0,
                  auction_volume: float = 0.0, preclose_volume: float = 0.0,
                  prev_high: float = 0.0, gap_price: Optional[float] = None,
                  ratio: float = 0.0, thresholds: Optional[dict] = None) -> Dict:
    """按战法判定买点是否触发。返回 {triggered, note}"""
    if tactic == 'weak_strong':
        if open_change_pct >= 3.0 and auction_volume > preclose_volume * 0.5:
            return {'triggered': True, 'note': '✅ 弱转强买点（高开≥3%+竞价放量）'}
        return {'triggered': False, 'note': '⏳ 弱转强未确认'}
    if tactic == 'graph':
        if gap_price and prev_high and gap_price > prev_high:
            return {'triggered': True, 'note': '✅ 图形突破买点（跳空过前高）'}
        return {'triggered': False, 'note': '⏳ 未跳空过压力位'}
    # auction 默认
    low = (thresholds or {}).get('auction_ratio_low', 3.0)
    if ratio >= low:
        return {'triggered': True, 'note': f'✅ 竞价爆量（{ratio:.1f}%）'}
    return {'triggered': False, 'note': '⏳ 竞价量能不足'}
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: 10 passed（8 + 2）

- [ ] **Step 5: 提交**

```bash
git add ashare_review/one_two_v2/auction.py ashare_review/tests/test_one_two_v2.py
git commit -m "feat(1in2): 竞价确认（爆量三档 + 战法联动）"
```

---

### Task 4: ledger.py 台账 + 维度统计

**Files:**
- Create: `ashare_review/one_two_v2/ledger.py`
- Test: `ashare_review/tests/test_one_two_v2.py`（追加）

- [ ] **Step 1: 写失败测试**

```python
# 追加到 test_one_two_v2.py

# ---------- Task 4: 台账 ----------

def test_ledger_record_and_verify(tmp_path):
    from ashare_review.one_two_v2.ledger import Ledger
    l = Ledger(str(tmp_path / 't.db'))
    pid = l.record_pick('20260831', '600001', '测试', 55.0,
                        {'quality': 20}, 'auction', mcap=40.0)
    assert pid > 0
    assert l.record_pick('20260831', '600001', '测试', 55.0, {}, 'auction') == 0  # 幂等
    n = l.verify_pick('20260831', '600001', 'zt', 1, 6.5)
    assert n == 1
    row = l.get_pick('20260831', '600001')
    assert row['next_result'] == 'zt' and row['hit'] == 1
    assert row['auction_ratio'] == 6.5


def test_ledger_dimension_stats(tmp_path):
    from ashare_review.one_two_v2.ledger import Ledger
    l = Ledger(str(tmp_path / 't.db'))
    # 5 条：3 条 quality>0（2 命中 1 未中），2 条 quality<=0（都未中）
    for i, (score, hit) in enumerate([(30, 1), (25, 1), (20, 0), (-5, 0), (0, 0)]):
        l.record_pick('20260831', f'6000{i:02d}', 'X', float(score),
                      {'quality': score}, 'auction')
        l.verify_pick('20260831', f'6000{i:02d}', 'zt' if hit else 'down', hit, 0)
    stats = l.dimension_stats()
    q = stats['quality']
    assert q['pos_total'] == 3 and q['pos_hit'] == 2
    assert q['neg_total'] == 2 and q['neg_hit'] == 0
    # 按战法
    t = stats['by_tactic']
    assert t['auction']['total'] == 5


def test_ledger_pending_verification(tmp_path):
    from ashare_review.one_two_v2.ledger import Ledger
    l = Ledger(str(tmp_path / 't.db'))
    l.record_pick('20260831', '600001', 'A', 50.0, {}, 'auction')
    l.record_pick('20260830', '600002', 'B', 45.0, {}, 'graph')
    pending = l.get_pending()
    assert len(pending) == 2
    assert pending[0]['pick_date'] == '20260830'   # 先验证更早的
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: FAIL — ModuleNotFoundError

- [ ] **Step 3: 实现 ledger.py**

```python
# ashare_review/one_two_v2/ledger.py
"""今日1进2 — SQLite 结果台账 + 维度命中率统计"""
import json
import os
import sqlite3
from typing import Dict, List, Optional

SCHEMA = """
CREATE TABLE IF NOT EXISTS picks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  pick_date TEXT NOT NULL,
  code TEXT NOT NULL,
  name TEXT DEFAULT '',
  score REAL,
  dimensions TEXT DEFAULT '{}',
  tactic TEXT DEFAULT 'auction',
  next_date TEXT,
  next_result TEXT,
  hit INTEGER,
  auction_ratio REAL,
  mcap REAL,
  created_at TEXT DEFAULT (datetime('now','localtime'))
);
CREATE UNIQUE INDEX IF NOT EXISTS uq_pick ON picks(pick_date, code);
CREATE INDEX IF NOT EXISTS idx_pick_date ON picks(pick_date);
"""


class Ledger:
    def __init__(self, db_path: str):
        self.db_path = db_path
        os.makedirs(os.path.dirname(db_path) or '.', exist_ok=True)
        self._init()

    def _conn(self) -> sqlite3.Connection:
        conn = sqlite3.connect(self.db_path, timeout=10)
        conn.row_factory = sqlite3.Row
        return conn

    def _init(self) -> None:
        conn = self._conn()
        try:
            conn.executescript(SCHEMA)
            conn.commit()
        finally:
            conn.close()

    def record_pick(self, pick_date: str, code: str, name: str, score: float,
                    dimensions: dict, tactic: str, mcap: Optional[float] = None) -> int:
        conn = self._conn()
        try:
            cur = conn.execute(
                "INSERT OR IGNORE INTO picks (pick_date, code, name, score, dimensions, tactic, mcap) "
                "VALUES (?, ?, ?, ?, ?, ?, ?)",
                (pick_date, code, name, score, json.dumps(dimensions, ensure_ascii=False), tactic, mcap))
            conn.commit()
            return cur.rowcount
        finally:
            conn.close()

    def verify_pick(self, pick_date: str, code: str, next_result: str,
                    hit: int, auction_ratio: Optional[float] = None) -> int:
        conn = self._conn()
        try:
            cur = conn.execute(
                "UPDATE picks SET next_result=?, hit=?, auction_ratio=? "
                "WHERE pick_date=? AND code=? AND hit IS NULL",
                (next_result, hit, auction_ratio, pick_date, code))
            conn.commit()
            return cur.rowcount
        finally:
            conn.close()

    def get_pick(self, pick_date: str, code: str) -> Optional[dict]:
        conn = self._conn()
        try:
            row = conn.execute(
                "SELECT * FROM picks WHERE pick_date=? AND code=?", (pick_date, code)).fetchone()
            return dict(row) if row else None
        finally:
            conn.close()

    def get_pending(self) -> List[dict]:
        conn = self._conn()
        try:
            rows = conn.execute(
                "SELECT * FROM picks WHERE hit IS NULL ORDER BY pick_date").fetchall()
            return [dict(r) for r in rows]
        finally:
            conn.close()

    def list_picks(self, pick_date: str) -> List[dict]:
        conn = self._conn()
        try:
            rows = conn.execute(
                "SELECT * FROM picks WHERE pick_date=? ORDER BY score DESC", (pick_date,)).fetchall()
            return [dict(r) for r in rows]
        finally:
            conn.close()

    def dimension_stats(self) -> Dict:
        """各维度：正分 vs 非正分的命中率对比 + 按战法统计"""
        conn = self._conn()
        try:
            rows = conn.execute(
                "SELECT dimensions, tactic, hit FROM picks WHERE hit IS NOT NULL").fetchall()
        finally:
            conn.close()
        dims = {}
        by_tactic = {}
        for r in rows:
            d = json.loads(r['dimensions'] or '{}')
            t = r['tactic'] or 'auction'
            b = by_tactic.setdefault(t, {'total': 0, 'hit': 0})
            b['total'] += 1
            if r['hit'] == 1:
                b['hit'] += 1
            for k, v in d.items():
                s = v.get('score', 0) if isinstance(v, dict) else v
                pos = 'pos' if s > 0 else 'neg'
                e = dims.setdefault(k, {'pos_total': 0, 'pos_hit': 0, 'neg_total': 0, 'neg_hit': 0})
                e[f'{pos}_total'] += 1
                if r['hit'] == 1:
                    e[f'{pos}_hit'] += 1
        for k in dims:
            e = dims[k]
            e['pos_rate'] = round(e['pos_hit'] / e['pos_total'], 4) if e['pos_total'] else None
            e['neg_rate'] = round(e['neg_hit'] / e['neg_total'], 4) if e['neg_total'] else None
        for t in by_tactic:
            b = by_tactic[t]
            b['rate'] = round(b['hit'] / b['total'], 4) if b['total'] else None
        return {'dimensions': dims, 'by_tactic': by_tactic}
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: 13 passed（10 + 3）

- [ ] **Step 5: 提交**

```bash
git add ashare_review/one_two_v2/ledger.py ashare_review/tests/test_one_two_v2.py
git commit -m "feat(1in2): 结果台账 + 维度命中率统计"
```

---

### Task 5: service.py 编排（run_picks / refresh_auction / verify_pending + 后台任务）

**Files:**
- Create: `ashare_review/one_two_v2/service.py`
- Test: `ashare_review/tests/test_one_two_v2.py`（追加）

- [ ] **Step 1: 写失败测试（用 mock 数据源，不触网）**

```python
# 追加到 test_one_two_v2.py

# ---------- Task 5: 编排 ----------

class FakePoolSource:
    """mock 数据源：涨停池/日线/概念/情绪/竞价"""
    def __init__(self):
        from datetime import date
        self.today = '20260831'
    def get_limit_up_pool(self, d=None):
        return [_lu(code='600001', t='09:40', seal_amt=6000, turnover=8000, cap=40.0),
                _lu(code='600002', t='10:10', seal_amt=3000, turnover=8000, cap=60.0)]
    def read_daily(self, code, market):
        import pandas as pd
        from datetime import datetime
        bars = [('2026-06-01', 10.0, 10.0)] * 60 + [('2026-08-31', 10.0, 10.9)]
        return pd.DataFrame([{'trade_date': datetime.strptime(d, '%Y-%m-%d').date(),
                              'open': o, 'high': o, 'low': c, 'close': c, 'volume': 500 + i,
                              'amount': 5000 + i * 10} for i, (d, o, c) in enumerate(bars)])
    def get_auction_data(self):
        return []
    def fetch_one_auction(self, code):
        return None


def test_run_picks(tmp_path, monkeypatch):
    import json
    from ashare_review.one_two_v2 import service as svc
    from ashare_review.one_two_v2.ledger import Ledger
    monkeypatch.setattr(svc, 'LEDGER_DB', str(tmp_path / 't.db'))
    monkeypatch.setattr(svc, '_fetch_ctx', lambda: ({}, {}))   # 简化上下文
    result = svc.run_picks(pool=[_lu(code='600001', t='09:40', seal_amt=6000, turnover=8000, cap=40.0)],
                           weights=svc.default_weights())
    assert result['total'] == 1
    assert result['picks'][0]['code'] == '600001'
    l = Ledger(str(tmp_path / 't.db'))
    assert len(l.list_picks('20260831')) == 1


def test_run_picks_empty_pool(tmp_path, monkeypatch):
    from ashare_review.one_two_v2 import service as svc
    monkeypatch.setattr(svc, 'LEDGER_DB', str(tmp_path / 't.db'))
    r = svc.run_picks(pool=[], weights=svc.default_weights())
    assert r['total'] == 0 and r['picks'] == []


def test_verify_pending(tmp_path, monkeypatch):
    from ashare_review.one_two_v2 import service as svc
    from ashare_review.one_two_v2.ledger import Ledger
    monkeypatch.setattr(svc, 'LEDGER_DB', str(tmp_path / 't.db'))
    l = Ledger(str(tmp_path / 't.db'))
    l.record_pick('20260828', '600001', 'A', 50.0, {'quality': {'score': 10}}, 'auction')
    # mock 验证器：次日 +10% → zt
    class FakeV:
        def verify_pick(self, pick_date, code, next_result, hit, auction_ratio):
            return l.verify_pick(pick_date, code, next_result, hit, auction_ratio)
    n = svc.verify_pending(verify_fake=lambda row: ('zt', 1, None), ledger=l)
    assert n == 1
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: FAIL — ModuleNotFoundError

- [ ] **Step 3: 实现 service.py**

```python
# ashare_review/one_two_v2/service.py
"""今日1进2 — 编排层（盘后精选/竞价刷新/次日验证 + 后台任务）"""
import os
import threading
import time
import uuid
from datetime import date, timedelta
from typing import Dict, List, Optional

from ..utils.calendar import TradingCalendar
from .ledger import Ledger
from .weights import DEFAULT_WEIGHTS, WeightStore
from . import picks as picks_mod
from . import auction as auction_mod

LEDGER_DB = os.environ.get(
    'ONE_TWO_LEDGER',
    os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
                 'data', 'one_two_ledger.db'))
JOBS: Dict[str, dict] = {}
_JOBS_LOCK = threading.Lock()


def default_weights() -> dict:
    return DEFAULT_WEIGHTS


def _today() -> str:
    return date.today().strftime('%Y%m%d')


def _prev_trade_date(calendar: TradingCalendar, d: str) -> str:
    dd = date(int(d[:4]), int(d[4:6]), int(d[6:8]))
    return calendar.prev_trading_day(dd, offset=1).strftime('%Y%m%d')


def run_picks(pool: List, weights: Optional[dict] = None,
              ctx: Optional[dict] = None, trade_date: Optional[str] = None,
              top_n: int = 8, ledger: Optional[Ledger] = None) -> Dict:
    """盘后精选：过滤候选 → 8 维打分 → 落库。返回 {total, picks}"""
    weights = weights or default_weights()
    td = trade_date or _today()
    cands = picks_mod.filter_candidates(pool or [])
    picks_out = []
    for c in cands[:50]:
        lu = c['lu']
        d = picks_mod.compute_score(lu, (ctx or {}).get('scored', {}).get(c['code'], {}), weights)
        picks_out.append({
            'code': c['code'], 'name': c['name'],
            'score': d['score'], 'dimensions': d['dimensions'], 'tactic': d['tactic'],
            'mcap': float(lu.float_market_cap or 0),
        })
    picks_out.sort(key=lambda x: x['score'], reverse=True)
    picks_out = picks_out[:top_n]
    if ledger:
        for p in picks_out:
            ledger.record_pick(td, p['code'], p['name'], p['score'],
                               p['dimensions'], p['tactic'], mcap=p.get('mcap'))
    return {'total': len(picks_out), 'picks': picks_out, 'date': td}


def verify_pending(ledger: Optional[Ledger] = None, verify_fake=None) -> int:
    """验证所有未验证记录（次日结果）。verify_fake 供测试注入。"""
    ledger = ledger or Ledger(LEDGER_DB)
    pending = ledger.get_pending()
    verified = 0
    for row in pending:
        try:
            if verify_fake:
                next_result, hit, ratio = verify_fake(row)
            else:
                next_result, hit, ratio = _verify_one(row)
            if next_result and hit is not None:
                ledger.verify_pick(row['pick_date'], row['code'], next_result, hit, ratio)
                verified += 1
        except Exception:
            continue
    return verified


def _verify_one(row) -> tuple:
    """真实验证：TDX 次日行情 + 次日涨停池（本任务占位，Task 8 接真实数据源）。"""
    return None, None, None


def start_job(kind: str, params: dict) -> str:
    job_id = uuid.uuid4().hex[:12]
    with _JOBS_LOCK:
        JOBS[job_id] = {'status': 'running', 'progress': '排队中', 'error': None}
    def _worker():
        try:
            with _JOBS_LOCK:
                JOBS[job_id]['progress'] = '运行中…'
            if kind == 'picks':
                from ..data.akshare_fetcher import AkshareFetcher
                pool = AkshareFetcher().get_limit_up_pool()
                r = run_picks(pool, trade_date=params.get('trade_date'))
                with _JOBS_LOCK:
                    JOBS[job_id]['result'] = r
            with _JOBS_LOCK:
                JOBS[job_id]['status'] = 'done'
        except Exception as e:
            with _JOBS_LOCK:
                JOBS[job_id]['status'] = 'error'
                JOBS[job_id]['error'] = str(e)
    threading.Thread(target=_worker, daemon=True).start()
    return job_id


def get_job(job_id: str) -> Optional[dict]:
    with _JOBS_LOCK:
        return dict(JOBS.get(job_id) or {}) or None
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: 16 passed（13 + 3）

- [ ] **Step 5: 提交**

```bash
git add ashare_review/one_two_v2/service.py ashare_review/tests/test_one_two_v2.py
git commit -m "feat(1in2): 编排层（盘后精选/次日验证/后台任务）"
```

---

### Task 6: app.py API + 导航

**Files:**
- Modify: `ashare_review/web/app.py`
- Modify: `ashare_review/web/templates/base.html`
- Test: `ashare_review/tests/test_one_two_v2.py`（追加）

- [ ] **Step 1: 写失败测试**

```python
# 追加到 test_one_two_v2.py

# ---------- Task 6: Web API ----------

def test_one_two_page_and_api(tmp_path, monkeypatch):
    from ashare_review.one_two_v2 import service as svc
    from ashare_review.one_two_v2.weights import WeightStore
    from ashare_review.web.app import app
    monkeypatch.setattr(svc, 'LEDGER_DB', str(tmp_path / 't.db'))
    monkeypatch.setattr(WeightStore, '_WEIGHTS_PATH', str(tmp_path / 'w.json'))
    app.config['TESTING'] = True
    c = app.test_client()
    rv = c.get('/one_two_picks')
    assert rv.status_code == 200
    body = rv.data.decode('utf-8')
    assert '今日1进2' in body
    # 权重 API
    rv2 = c.get('/api/one_two/weights')
    assert rv2.status_code == 200
    assert rv2.get_json()['dimensions']['quality'] == 30
    # 保存
    rv3 = c.post('/api/one_two/weights', json={'dimensions': {'quality': 40}})
    assert rv3.status_code == 200
    # 非法
    rv4 = c.post('/api/one_two/weights', json={'dimensions': {'quality': 99}})
    assert rv4.status_code == 400
    # ledger stats
    rv5 = c.get('/api/one_two/ledger/stats')
    assert rv5.status_code == 200
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: FAIL — 404

- [ ] **Step 3: 实现路由 + 导航**

在 `app.py` 末尾加：

```python
# ======================================================================
# 今日1进2（视频方法论版）
# ======================================================================

@app.route('/one_two_picks')
def one_two_picks():
    from ..one_two_v2.service import LEDGER_DB
    from ..one_two_v2.ledger import Ledger
    from ..one_two_v2.weights import WeightStore
    ledger = Ledger(LEDGER_DB)
    return render_template('one_two_picks.html',
                           weights=WeightStore().get(),
                           today_picks=ledger.list_picks(''),
                           stats=ledger.dimension_stats())


@app.route('/api/one_two/picks/run', methods=['POST'])
def api_one_two_picks_run():
    from ..one_two_v2.service import start_job
    data = request.get_json(silent=True) or {}
    job_id = start_job('picks', {'trade_date': data.get('trade_date') or ''})
    return jsonify({'ok': True, 'job_id': job_id})


@app.route('/api/one_two/picks')
def api_one_two_picks():
    from ..one_two_v2.service import LEDGER_DB, _today
    from ..one_two_v2.ledger import Ledger
    d = request.args.get('date') or _today()
    return jsonify({'picks': Ledger(LEDGER_DB).list_picks(d), 'date': d})


@app.route('/api/one_two/ledger/stats')
def api_one_two_ledger_stats():
    from ..one_two_v2.service import LEDGER_DB
    from ..one_two_v2.ledger import Ledger
    return jsonify(Ledger(LEDGER_DB).dimension_stats())


@app.route('/api/one_two/weights')
def api_one_two_weights():
    from ..one_two_v2.weights import WeightStore
    return jsonify(WeightStore().get())


@app.route('/api/one_two/weights', methods=['POST'])
def api_one_two_weights_save():
    from ..one_two_v2.weights import WeightStore
    data = request.get_json(silent=True) or {}
    try:
        WeightStore().set(data)
        return jsonify({'ok': True})
    except ValueError as e:
        return jsonify({'ok': False, 'error': str(e)}), 400


@app.route('/api/one_two/auction/refresh', methods=['POST'])
def api_one_two_auction_refresh():
    from ..one_two_v2.auction import grade_auction_ratio
    from ..one_two_v2.service import LEDGER_DB
    from ..one_two_v2.ledger import Ledger
    from ..one_two_v2.weights import WeightStore
    from ..data.akshare_fetcher import AkshareFetcher
    try:
        ak = AkshareFetcher()
        auctions = {a.code: a for a in ak.get_auction_data()}
    except Exception as e:
        return jsonify({'ok': False, 'error': str(e)}), 502
    th = WeightStore().get()['thresholds']
    ledger = Ledger(LEDGER_DB)
    prev = _prev_trade_date_for_auction()
    rows = ledger.list_picks(prev) or ledger.list_picks('')
    out = []
    for r in rows:
        a = auctions.get(r['code'])
        if not a:
            continue
        g = grade_auction_ratio(a.auction_amount, float(r.get('mcap') or 0), th)
        out.append({'code': r['code'], 'name': r['name'], 'tactic': r['tactic'],
                    'open_change_pct': a.open_change_pct,
                    'auction_amount': a.auction_amount,
                    'ratio': g['ratio'], 'level': g['level'], 'label': g['label']})
    return jsonify({'ok': True, 'items': out})


def _prev_trade_date_for_auction() -> str:
    from ..utils.calendar import TradingCalendar
    from datetime import date
    return TradingCalendar().prev_trading_day(date.today(), offset=1).strftime('%Y%m%d')


```

在 `base.html` 交易流程导航"多策略选股"之后加：

```html
            <a href="/one_two_picks" class="nav-item {% if request.endpoint == 'one_two_picks' %}active{% endif %}">
                <span class="nav-icon">🧩</span>
                <span class="nav-label">今日1进2</span>
            </a>
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py -v`
Expected: 17 passed（16 + 1）

- [ ] **Step 5: 提交**

```bash
git add ashare_review/web/app.py ashare_review/web/templates/base.html ashare_review/tests/test_one_two_v2.py
git commit -m "feat(1in2): API 与导航（页面/精选/竞价/台账/权重）"
```

---

### Task 7: one_two_picks.html 页面

**Files:**
- Create: `ashare_review/web/templates/one_two_picks.html`

- [ ] **Step 1: 写模板**（四区块：今日精选 / 竞价确认 / 战绩 / 权重弹窗）

```html
{% extends "base.html" %}
{% block title %}今日1进2 · 竞价交易系统{% endblock %}
{% block content %}
<div class="content-area">
    <div class="page-header">
        <div>
            <div class="page-title">🧩 今日1进2</div>
            <div class="page-date">盘后精选（视频方法论 8 维）→ 次日竞价确认 → 实盘结果校准</div>
        </div>
        <div>
            <button class="btn btn-primary" onclick="runPicks()">⚡ 盘后精选</button>
            <button class="btn btn-secondary" onclick="refreshAuction()">🔔 刷新竞价</button>
            <button class="btn btn-secondary" onclick="openWeights()">⚙️ 权重</button>
        </div>
    </div>

    <!-- 📋 今日精选 -->
    <div class="section-title">📋 今日精选（盘后）</div>
    <div class="card">
        <div class="card-body no-padding">
            <div class="table-wrap">
                <table class="sector-table">
                    <tr><th>代码</th><th>名称</th><th>总分</th><th>战法</th><th>质量</th><th>题材</th>
                        <th>情绪</th><th>梯队</th><th>量能</th><th>叠加</th><th>市值</th><th>地位</th></tr>
                    <tbody>
                    {% for p in today_picks %}
                    <tr>
                        <td>{{ p.code }}</td><td>{{ p.name }}</td>
                        <td><strong>{{ p.score }}</strong></td>
                        <td>{{ {'auction':'竞价爆量','graph':'图形突破','weak_strong':'弱转强'}.get(p.tactic, p.tactic) }}</td>
                        {% set d = p.dimensions %}
                        <td>{{ d.get('quality', {}).get('score', '—') if d is mapping else '—' }}</td>
                        <td>{{ d.get('theme_stage', {}).get('score', '—') if d is mapping else '—' }}</td>
                        <td>{{ d.get('emotion', {}).get('score', '—') if d is mapping else '—' }}</td>
                        <td>{{ d.get('energy_ladder', {}).get('score', '—') if d is mapping else '—' }}</td>
                        <td>{{ d.get('volume_health', {}).get('score', '—') if d is mapping else '—' }}</td>
                        <td>{{ d.get('theme_overlay', {}).get('score', '—') if d is mapping else '—' }}</td>
                        <td>{{ d.get('cap_price', {}).get('score', '—') if d is mapping else '—' }}</td>
                        <td>{{ d.get('status', {}).get('score', '—') if d is mapping else '—' }}</td>
                    </tr>
                    {% endfor %}
                    {% if not today_picks %}
                    <tr><td colspan="12" style="color:#888;text-align:center;">暂无精选，点击「⚡ 盘后精选」生成</td></tr>
                    {% endif %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- 🔔 竞价确认 -->
    <div class="section-title">🔔 竞价确认（次日 9:25 后）</div>
    <div class="card">
        <div class="card-body no-padding">
            <div class="table-wrap">
                <table class="sector-table">
                    <tr><th>代码</th><th>名称</th><th>战法</th><th>竞价涨幅</th><th>爆量比</th><th>信号</th></tr>
                    <tbody id="auction-body">
                        <tr><td colspan="6" style="color:#888;text-align:center;">点「🔔 刷新竞价」拉取数据</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- 📊 战绩 -->
    <div class="section-title">📊 战绩（实盘结果校准）</div>
    <div class="card">
        <div class="card-body no-padding">
            <div class="table-wrap">
                <table class="sector-table">
                    <tr><th>维度</th><th>正分样本/命中</th><th>正分命中率</th><th>非正分样本/命中</th><th>非正分命中率</th></tr>
                    <tbody>
                    {% for dim, st in stats.dimensions.items() %}
                    <tr>
                        <td>{{ {'quality':'质量','theme_stage':'题材阶段','emotion':'情绪周期','energy_ladder':'高能量梯队',
                                'volume_health':'合理量能','theme_overlay':'题材叠加','cap_price':'市值票值','status':'地位'}.get(dim, dim) }}</td>
                        <td>{{ st.pos_hit }}/{{ st.pos_total }}</td>
                        <td>{{ '%.0f%%' % (st.pos_rate*100) if st.pos_rate is not none else '—' }}</td>
                        <td>{{ st.neg_hit }}/{{ st.neg_total }}</td>
                        <td>{{ '%.0f%%' % (st.neg_rate*100) if st.neg_rate is not none else '—' }}</td>
                    </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<!-- ⚙️ 权重弹窗 -->
<div id="weights-modal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.35);z-index:99;align-items:center;justify-content:center;">
    <div style="background:#fff;border-radius:14px;padding:20px;width:480px;max-width:92vw;">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px;">
            <strong>⚙️ 打分权重（保存后次日盘后生效）</strong>
            <button class="btn btn-secondary btn-sm" onclick="closeWeights()">✕</button>
        </div>
        <div id="weights-form" style="display:flex;flex-direction:column;gap:8px;"></div>
        <div style="margin-top:14px;display:flex;gap:10px;justify-content:flex-end;">
            <button class="btn btn-secondary" onclick="closeWeights()">取消</button>
            <button class="btn btn-primary" onclick="saveWeights()">保存</button>
        </div>
    </div>
</div>
{% endblock %}
{% block scripts %}
<script>
var DIM_LABELS = {'quality':'质量','theme_stage':'题材阶段','emotion':'情绪周期','energy_ladder':'高能量梯队',
    'volume_health':'合理量能','theme_overlay':'题材叠加','cap_price':'市值票值','status':'地位'};

function runPicks() {
    fetch('/api/one_two/picks/run', {method: 'POST', headers: {'Content-Type': 'application/json'}, body: '{}'})
        .then(function (r) { return r.json(); })
        .then(function (d) { if (d.ok) { alert('盘后精选已启动'); setTimeout(function(){ location.reload(); }, 3000); } });
}
function refreshAuction() {
    fetch('/api/one_two/auction/refresh', {method: 'POST'})
        .then(function (r) { return r.json(); })
        .then(function (d) {
            var body = document.getElementById('auction-body');
            if (!d.ok) { body.innerHTML = '<tr><td colspan="6" style="color:#dc2626;">' + (d.error || '失败') + '</td></tr>'; return; }
            if (!d.items.length) { body.innerHTML = '<tr><td colspan="6" style="color:#888;">今日无竞价数据（可能尚未到 9:25）</td></tr>'; return; }
            body.innerHTML = '';
            d.items.forEach(function (it) {
                var tr = document.createElement('tr');
                tr.innerHTML = '<td>' + it.code + '</td><td>' + it.name + '</td><td>' +
                    (DIM_LABELS[it.tactic] || it.tactic) + '</td><td>' +
                    (it.open_change_pct !== null && it.open_change_pct !== undefined ? it.open_change_pct.toFixed(1) + '%' : '—') +
                    '</td><td>' + it.ratio.toFixed(1) + '%</td><td>' + it.label + '</td>';
                body.appendChild(tr);
            });
        });
}
function openWeights() {
    fetch('/api/one_two/weights')
        .then(function (r) { return r.json(); })
        .then(function (w) {
            var box = document.getElementById('weights-form');
            box.innerHTML = '';
            Object.keys(w.dimensions).forEach(function (k) {
                var row = document.createElement('div');
                row.style.cssText = 'display:flex;justify-content:space-between;align-items:center;';
                row.innerHTML = '<span style="font-size:.9em;">' + (DIM_LABELS[k] || k) + '</span>' +
                    '<input data-dim="' + k + '" type="number" value="' + w.dimensions[k] + '" style="width:100px;padding:6px 8px;border:1px solid #e2e8f0;border-radius:6px;">';
                box.appendChild(row);
            });
            document.getElementById('weights-modal').style.display = 'flex';
        });
}
function saveWeights() {
    var dims = {};
    document.querySelectorAll('#weights-form [data-dim]').forEach(function (el) { dims[el.dataset.dim] = Number(el.value); });
    fetch('/api/one_two/weights', {method: 'POST', headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({dimensions: dims})})
        .then(function (r) { return r.json(); })
        .then(function (d) { if (!d.ok) { alert('保存失败: ' + (d.error || '')); return; } closeWeights(); alert('权重已保存，次日盘后选股生效'); });
}
function closeWeights() { document.getElementById('weights-modal').style.display = 'none'; }
</script>
{% endblock %}
```

- [ ] **Step 2: 冒烟测试**

Run: `python -m pytest ashare_review/tests/test_one_two_v2.py::test_one_two_page_and_api -v`
Expected: PASS（页面 200）

- [ ] **Step 3: 提交**

```bash
git add ashare_review/web/templates/one_two_picks.html
git commit -m "feat(1in2): 页面（精选/竞价/战绩/权重）"
```

---

### Task 8: 真实数据源接线 + 全量回归 + 冒烟 + 推送

- [ ] **Step 1: 接线真实数据源（build_pick_context + _verify_one，替换占位）**

在 `service.py` 实现两个真实数据函数：

```python
def build_pick_context(pool: List, tdx=None, calendar=None,
                       concept_map: Optional[dict] = None,
                       state_df=None) -> Dict:
    """组装 8 维打分所需上下文（板块聚合/高能量梯队/情绪趋势/前高量/概念叠加/地位）。

    返回 {"scored": {code: {sector, zt_trend, ladder_at_2, ladder_at_3,
                            today_vol, prev_high_vol, concept_count, upper_same_theme}}}
    """
    calendar = calendar or TradingCalendar()
    concept_map = concept_map or {}
    sector = {}
    for lu in pool or []:
        ind = str(getattr(lu, 'board_type', '') or '未知')
        s = sector.setdefault(ind, {'zt_count': 0, 'max_consecutive': 1, 'is_new_theme': False})
        s['zt_count'] += 1
        s['max_consecutive'] = max(s['max_consecutive'], lu.consecutive or 1)
    ladder = {2: 0, 3: 0, 4: 0}
    for lu in pool or []:
        c = lu.consecutive or 1
        if 2 <= c <= 4:
            try:
                if str(lu.limit_up_time).replace(':', '')[:4] == '0925':
                    ladder[c] += 1
            except Exception:
                pass
    ladder_at_2 = ladder[2] >= max(ladder[3], ladder[4], 1)
    ladder_at_3 = ladder[3] > ladder[2] and ladder[3] >= max(ladder[2], ladder[4], 1)
    zt_trend = 'neutral'
    if state_df is not None and len(state_df) >= 3:
        zts = list(state_df['limit_up'].dropna().tail(3))
        if len(zts) >= 3 and zts[-1] < zts[-2] < zts[-3]:
            zt_trend = 'double_ice'
        elif len(zts) >= 3 and zts[-1] > zts[-2] > zts[-3]:
            zt_trend = 'double_climax'
    upper_concepts = set()
    for lu in pool or []:
        if (lu.consecutive or 1) >= 2:
            for cn, info in (concept_map or {}).items():
                if str(lu.code) in (info.get('members') or {}):
                    upper_concepts.add(cn)
    code_to_concepts = {}
    for lu in pool or []:
        hits = [cn for cn, info in (concept_map or {}).items()
                if str(lu.code) in (info.get('members') or {})]
        code_to_concepts[str(lu.code)] = hits
    scored = {}
    for lu in pool or []:
        code = str(lu.code)
        ind = str(getattr(lu, 'board_type', '') or '未知')
        scored[code] = {
            'sector': sector.get(ind, {}),
            'zt_trend': zt_trend,
            'ladder_at_2': ladder_at_2,
            'ladder_at_3': ladder_at_3,
            'today_vol': tv, 'prev_high_vol': pv,   # 已接 TDX 真实量能（_volume_health_data）
            'concept_count': len(code_to_concepts.get(code, [])),
            'upper_same_theme': bool(set(code_to_concepts.get(code, [])) & upper_concepts),
        }
    return {'scored': scored}


def _grade_next_day(prev_close: float, close: float, code: str) -> tuple:
    """纯分级：次日涨幅 → (next_result, hit)。zt 或涨≥3% 记命中。"""
    chg = (close - prev_close) / prev_close * 100 if prev_close else 0
    limit = 19.6 if code.startswith(('30', '68')) else (29.4 if code.startswith(('8', '4', '92')) else 9.8)
    if chg >= limit:
        return 'zt', 1
    if chg >= 3:
        return 'up3', 1
    if chg >= 0:
        return 'up', 0
    if chg >= -3:
        return 'flat', 0
    return 'down', 0


def _verify_one(row) -> tuple:
    """真实验证：TDX 次日行情 → (next_result, hit, auction_ratio)。"""
    from datetime import datetime
    from ..data.tdx_reader import TdxReader
    from ..utils.calendar import TradingCalendar
    tdx = TdxReader()
    cal = TradingCalendar()
    d = datetime.strptime(row['pick_date'], '%Y%m%d').date()
    nxt = cal.next_trading_day(d, offset=1)
    if nxt is None:
        return None, None, None
    code = row['code']
    market = 'bj' if code.startswith(('8', '4', '92')) else ('sh' if code.startswith('6') else 'sz')
    try:
        df = tdx.read_daily(code, market)
        if df is None or df.empty:
            return None, None, None
        df = df.reset_index(drop=True)
        mask = df['trade_date'] == nxt
        if not mask.any():
            return None, None, None
        pos = int(mask.idxmax())
        if pos == 0:
            return None, None, None
        prev_c = float(df.iloc[pos - 1]['close'])
        close = float(df.iloc[pos]['close'])
        result, hit = _grade_next_day(prev_c, close, code)
        return result, hit, None
    except Exception:
        return None, None, None
```

追加测试（Task 8 区）：

```python
def test_grade_next_day():
    from ashare_review.one_two_v2.service import _grade_next_day
    assert _grade_next_day(10.0, 11.0, '600001') == ('zt', 1)     # +10% 主板涨停
    assert _grade_next_day(10.0, 10.3, '600001') == ('up3', 1)    # +3% 命中
    assert _grade_next_day(10.0, 9.7, '600001') == ('flat', 0)    # -3%
    assert _grade_next_day(10.0, 9.0, '600001') == ('down', 0)
    assert _grade_next_day(10.0, 12.0, '300001') == ('zt', 1)     # 创业板 20% 内不是涨停
    assert _grade_next_day(10.0, 12.2, '300001') == ('zt', 1)     # +22% ≥19.6


def test_build_pick_context():
    from ashare_review.one_two_v2.service import build_pick_context
    pool = [
        _lu(code='600001', t='09:40', board_type='半导体'),   # 首板
        _lu(code='600002', cons=2, t='09:25'),                # 2板一字（高能量梯队+高位）
        _lu(code='600003', cons=3, t='09:25'),                # 3板一字
    ]
    import pandas as pd
    state_df = pd.DataFrame({'limit_up': [80, 60, 40]})       # 连续下降 → 二连冰
    concept_map = {'半导体': {'members': {'600001': 1, '600002': 1}}}
    ctx = build_pick_context(pool, concept_map=concept_map, state_df=state_df)
    s1 = ctx['scored']['600001']
    assert s1['zt_trend'] == 'double_ice'
    assert s1['concept_count'] == 1
    assert s1['upper_same_theme'] is True       # 600002(2板) 同属半导体
    assert s1['ladder_at_2'] is True or s1['ladder_at_3'] is True
```

- [ ] **Step 2: 全量测试**

Run: `python -m pytest ashare_review/tests -q`
Expected: 全部通过（126 既有 + 17 新增 = 143 passed）

- [ ] **Step 3: 真实冒烟**

Run（盘后精选，真实涨停池 + TDX）：
```bash
python -c "from ashare_review.one_two_v2.service import run_picks; from ashare_review.data.akshare_fetcher import AkshareFetcher; r = run_picks(AkshareFetcher().get_limit_up_pool()); print('picks:', r['total']); [print(p['code'], p['score'], p['tactic']) for p in r['picks'][:5]]"
```
Expected: 正常输出精选（数量取决于当日首板数）；无异常

- [ ] **Step 4: 提交推送**

```bash
git status
git add ashare_review/one_two_v2 ashare_review/web/app.py ashare_review/web/templates/base.html ashare_review/web/templates/one_two_picks.html ashare_review/tests/test_one_two_v2.py
git commit -m "feat(1in2): 今日1进2 完整交付（视频方法论）"
git push origin main
```
